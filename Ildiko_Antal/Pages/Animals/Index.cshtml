@page
@model Ildiko_Antal.Pages.Animals.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Animale[0].nume)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Animale[0].regim)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Animale[0].altedetalii)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Animale[0].checkin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Animale[0].Specie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Animale[0].Adapost)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Animale) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nume)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.regim)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.altedetalii)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.checkin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Specie.denumirespecie)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Adapost.adresaadapost)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
